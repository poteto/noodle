Back to [[archive/plans/28-rename-prioritize-to-schedule/overview]]

# Phase 7: Update docs, brain, and generated code

## Goal

Update all documentation, brain notes, and generated code templates so no source-level reference to "prioritize" remains. After this phase, the full-codebase grep from the overview verification returns zero hits.

## Changes

### `README.md`

- Line 32: `"Prioritize: prioritizes queue from mise data"` becomes `"Schedule: schedules queue from mise data"`
- Lines 49-50: TOML example `[prioritize]` / `skill = "prioritize"` becomes `[schedule]` / `skill = "schedule"`

### `brain/todos.md`

- Any references to "prioritize" in todo descriptions become "schedule"

### `generate/skill_noodle.go`

- Config field description map: keys `"prioritize.skill"`, `"prioritize.run"`, `"prioritize.model"` become `"schedule.skill"`, `"schedule.run"`, `"schedule.model"`, descriptions updated ("scheduling sessions" wording is already correct, just fix the key prefix)
- Noodle skill documentation template (line 227): `"The prioritize skill reads"` becomes `"The schedule skill reads"`
- Table row (line 242): `"Hint for the prioritize skill"` becomes `"Hint for the schedule skill"`

### `internal/schemadoc/specs.go`

- Line 38: description `"Input to prioritize; generated by \`noodle mise\`."` becomes `"Input to schedule; generated by \`noodle mise\`." `
- Line 100: description `"Output of prioritize (written to queue-next.json); promoted to queue.json by the loop."` becomes `"Output of schedule (written to queue-next.json); promoted to queue.json by the loop."`

### `dispatcher/types.go`

- Line 23: comment `"prioritize"` in task type key example becomes `"schedule"`

### `dispatcher/preamble.go`

- Line 10: `"Prioritized work queue"` — this is English usage describing the queue's nature (items are prioritized), not the task type name. No rename needed; exclude from verification grep.

### `brain/vision/slides`

- References to "prioritize" in slide content are presentation artifacts. Out of scope for this rename — exclude from verification grep.

### `generate/skill_noodle_test.go`

- Assertions that check for "prioritize" in generated output become "schedule"

## Data structures

No struct changes. Only string content in docs and templates.

## Routing

| Provider | Model | Rationale |
|----------|-------|-----------|
| `codex` | `gpt-5.4` | Mechanical string replacements in docs and templates |

## Verification

### Static
```bash
go test ./... && go vet ./...
sh scripts/lint-arch.sh
```

### Runtime
- Full-codebase verification from overview: `grep -r 'prioritize' --include='*.go' --include='*.toml' --include='*.md' .` scoped to source (excluding worktrees, sessions, archived plans, and this plan's own files) returns zero hits
- `go generate ./generate/...` produces output with "schedule" references, no "prioritize"
- `go generate ./generate/...` must run as part of this phase's static verification (not just runtime) to ensure the on-disk `.agents/skills/noodle/SKILL.md` is regenerated with "schedule" references
