# mise.json Schema

Input to the prioritize skill. Generated by `noodle mise`.

```json
{
  "generated_at": "ISO 8601 timestamp",
  "backlog": [
    {
      "id": "string — unique item ID",
      "title": "string",
      "description": "string",
      "section": "string — backlog section grouping",
      "status": "string — open|in_progress|done",
      "tags": ["string"],
      "plan": "string|null — path to linked plan directory",
      "plan_phase": "string|null — current phase filename",
      "estimate": "string|null — size estimate"
    }
  ],
  "plans": [
    {
      "id": "string — plan ID (matches backlog item ID)",
      "title": "string",
      "status": "string — draft|active|complete",
      "directory": "string — path to plan directory",
      "phases": [
        {
          "name": "string — phase display name",
          "filename": "string — phase file basename",
          "status": "string — pending|in_progress|complete"
        }
      ]
    }
  ],
  "active_cooks": [
    {
      "id": "string — cook session ID",
      "target": "string — backlog item ID being worked",
      "provider": "string — claude|codex",
      "model": "string — model ID",
      "status": "string — running|completed|failed",
      "cost": "number — USD spent",
      "duration_s": "number — elapsed seconds"
    }
  ],
  "tickets": [],
  "resources": {
    "max_cooks": "number — concurrent cook limit",
    "active": "number — currently running",
    "available": "number — max_cooks - active"
  },
  "recent_history": [
    {
      "target": "string — backlog item ID",
      "cook_id": "string",
      "outcome": "string — success|failure|rejected",
      "cost": "number",
      "duration_s": "number"
    }
  ],
  "routing": {
    "defaults": {
      "provider": "string",
      "model": "string"
    },
    "tags": {}
  },
  "task_types": [
    {
      "key": "string — prioritize|execute|reflect|meditate|oops|debate|quality",
      "blocking": "boolean — blocks other work when pending",
      "schedule": "string — when to schedule this type"
    }
  ],
  "quality_verdicts": [
    {
      "session_id": "string",
      "accept": "boolean",
      "feedback": "string",
      "reviewed_at": "ISO 8601 timestamp"
    }
  ],
  "warnings": ["string — operational warnings"]
}
```
