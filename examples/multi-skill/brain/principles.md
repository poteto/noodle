# Principles

- Test before deploy. Never ship code that has not passed the test suite.
- Small changes. Each commit should be one logical change, independently reviewable.
- Conventional commits. All commit messages follow the `<type>(<scope>): <description>` format.
