# Commit

Ship verified work with clear conventional commits.

Rules:
- Use conventional commit header + meaningful body.
- Keep commits logical and reviewable.
- Merge verified worktree changes back to `main` for MVP shipping.
- If merge fails, report exact conflict/error and next action.
