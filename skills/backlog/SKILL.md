# Backlog

Operate the configured backlog adapter safely.

Defaults:
- Backlog lives in `brain/todos.md`.
- Use adapter scripts for `sync`, `add`, `done`, `edit`.

Guidance:
- Keep titles concise and specific.
- Preserve section organization.
- Use tags (`#frontend`, `#tests`) when they help routing.
